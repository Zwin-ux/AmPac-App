# AmPac Brain - Production Environment Template
# Copy this to .env.production and configure for your production environment

# ============================================
# Server Config
# ============================================
PROJECT_NAME="AmPac Brain 🧠"
VERSION="0.1.0"
API_V1_STR="/api/v1"

# CRITICAL: Set to "production" for production deployment
ENV=production

# ============================================
# Groq AI Configuration (Primary Provider)
# ============================================
GROQ_API_KEY=your-production-groq-api-key
GROQ_MODEL=llama3-8b-8192

# ============================================
# Firebase Configuration (Required)
# Use either FIREBASE_CREDENTIALS_PATH or FIREBASE_CREDENTIALS_JSON
# ============================================
FIREBASE_CREDENTIALS_PATH=serviceAccountKey.json
# FIREBASE_CREDENTIALS_JSON={"type":"service_account",...}

# ============================================
# Microsoft Graph (Optional - for calendar integration)
# ============================================
AZURE_TENANT_ID=
AZURE_CLIENT_ID=
AZURE_CLIENT_SECRET=

# ============================================
# Ventures API (Optional - for loan data sync)
# ============================================
VENTURES_USERNAME=
VENTURES_PASSWORD=
VENTURES_MOCK_MODE=False

# ============================================
# ShareFile (Optional - for document storage)
# ============================================
SHAREFILE_CLIENT_ID=
SHAREFILE_CLIENT_SECRET=
SHAREFILE_SUBDOMAIN=ampac

# ============================================
# Stripe Payment Processing (Required for production)
# ============================================
STRIPE_SECRET_KEY=sk_live_your-stripe-secret-key
STRIPE_WEBHOOK_SECRET=whsec_your-webhook-secret

# ============================================
# Feature Flags
# ============================================
GRAPH_ENABLED=True
VENTURES_ENABLED=True
SHAREFILE_ENABLED=True
BOOKINGS_ENABLED=True

# ============================================
# Security (Required - MUST CHANGE FROM DEFAULTS)
# ============================================
# CRITICAL: AUTH_DISABLED must be False in production
AUTH_DISABLED=False

# CRITICAL: Generate a secure random key (32+ chars)
# Example: openssl rand -base64 32
BRAIN_API_KEY=your-secure-api-key-min-32-chars

# CRITICAL: Generate a secure JWT secret
# Example: openssl rand -hex 32
JWT_SECRET=your-secure-jwt-secret-must-change

# ============================================
# Error Tracking (Recommended)
# ============================================
SENTRY_DSN=https://your-sentry-dsn@sentry.io/project

# ============================================
# CORS (Restrict in production)
# ============================================
ALLOWED_CORS_ORIGINS=["https://your-console-domain.com","https://your-mobile-domain.com"]

# ============================================
# Notifications (Optional)
# ============================================
TEAMS_WEBHOOK_URL=

# ============================================
# Storage (Optional)
# ============================================
STORAGE_BUCKET=your-storage-bucket
